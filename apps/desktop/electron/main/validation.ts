/**
 * Shared input-validation helpers. The actual implementation lives in
 * @code-review/core so it is unit-testable without booting Electron.
 */
export { assertLocalUrl } from '@code-review/core';
